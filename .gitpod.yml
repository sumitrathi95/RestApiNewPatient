image:
  file: .gitpod.Dockerfile
tasks:
  - init: echo "Replace me with a build script for the project."
    command: mkdir -p /workspace/data && mongod --port 27018 --dbpath /workspace/data &
  - init: echo "Setting Up Kafka and Zookeeper"
    command: sh build.sh
